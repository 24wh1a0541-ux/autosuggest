# autosuggest
to verify my git setup:

to verify the commits.

**git ui:**

1. add(+ button)
2. commit with message.
3. push/sync 

**git commands:**

1. git add -A
2. git commit -m "some message"
3. git push